{
  "name": "hard-scope-challenge",
  "private": true,
  "description": "Monorepo with Vite + Mantine frontend and Express backend",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend"
  }
}
